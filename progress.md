# Project Progress

## Phase 1: Project Initialization & Core Setup
- **Next.js Initialization:** Created a new Next.js 15 App Router project using TypeScript and Tailwind CSS v4.
- **Dependencies:** Installed required packages: `next-intl`, `framer-motion`, `zod`, `react-hook-form`, `lucide-react`, and `next-sitemap`.
- **CSS Architecture:** Configured `globals.css` with Tailwind v4 `@theme` block defining the "Royal Trust" color palette (`--color-navy`, `--color-gold`, etc.) and basic animations (`scrollReveal`).
- **Typography:** Set up optimized custom Google Fonts (`Playfair_Display`, `Inter`, and `Noto_Sans_Devanagari`) in the localized root layout to prevent layout shift.
- **Internationalization (i18n):**
  - Set up `next-intl` configuration (`src/i18n/request.ts` and `src/i18n/routing.ts`).
  - Set up routing middleware (`src/middleware.ts`) to handle `/en` and `/hi` paths.
  - Created base translation dictionaries: `messages/en.json` and `messages/hi.json` with keys for navigation, hero section, practice areas, stats, and common buttons.
  - Migrated `next.config.ts` to include the `next-intl` plugin.
- **Base Layout & Page:** Removed the default Next.js files and created the locale-aware `src/app/[locale]/layout.tsx` and a basic `src/app/[locale]/page.tsx`.

## Phase 2: Base Layout & SEO Foundation
- **Layout Components:** Extracted HTML and styles from the prototype to build reusable layout components:
  - `TopBar`: Contact details and "Book Now" prompt.
  - `Navbar`: Responsive navigation bar with mobile menu and sticky scroll effect.
  - `Footer`: Comprehensive site footer with practice area links, quick links, and BCI compliance tag.
  - `WhatsAppFloat`: Floating WhatsApp button with pulse animation and pre-filled message.
- **Integration:** Integrated these components into `src/app/[locale]/layout.tsx` wrapping the `children`.
- **Foundational SEO:**
  - Configured `generateMetadata` in root layout for dynamic Open Graph tags, canonical URLs, and hreflang (`en-IN`, `hi-IN`, `x-default`).
  - Added Google Search Console verification placeholder.
  - Created `public/robots.txt` allowing all traffic and pointing to sitemaps.
  - Configured `next-sitemap.config.js` to automatically handle localized routes and blog priorities.

## Phase 3: Home Page Implementation
- **Scroll Animations:** Created a reusable `ScrollReveal` component using `framer-motion` to handle scroll animations consistently based on the `whileInView` API (replacing the manual Intersection Observer in vanilla JS).
- **Home Page Sections:** Componentized the homepage sections:
  - `ServiceCarousel`: A responsive carousel of primary services with auto-play and manual controls.
  - `HeroSection`: Primary hero with localized titles, stats, and a sticky "Hero Card" representing the advocate.
  - `TrustStrip`: A strip of trust indicators (Enrolled, 500+ Clients, Transparent Fees).
  - `PracticeAreas`: A responsive grid showcasing primary practice areas (Criminal, Civil, Family, Police Station).
  - `WhyChoose`: Advantage section highlighting direct access and result-oriented strategies.
  - `ProcessSection`: Step-by-step consultation process and fee structure card.
  - `Testimonials`: Client reviews layout with star ratings.
  - `FAQSection`: Interactive FAQ accordion with an adjacent contact sidebar.
  - `LocationSection`: Chamber address details and map placeholder.
  - `CTABanner`: High-conversion call-to-action banner.
- **Integration:** Assembled all components logically in `src/app/[locale]/page.tsx`.
- **Fixes for Next.js 15:** 
  - Updated `layout.tsx` to handle `params` as a Promise (Next.js 15 routing change).
  - Renamed `middleware.ts` to `proxy.ts` according to the new Next.js 16.2 deprecations.
  - Adjusted `getRequestConfig` in `i18n/request.ts` to correctly return `locale: locale as string`.

## Phase 4: Practice Areas & Schema Markup
- **Localized Pages:** Created dynamic routes at `src/app/[locale]/practice-areas/[area]/page.tsx` utilizing translations injected via node script to `en.json` and `hi.json` for detailed insights.
- **Dynamic Content & SEO:** 
  - Dynamic `generateMetadata` implemented to provide distinct `<title>`, description, Open Graph, and hreflang links based on the loaded practice area.
  - Added placeholders for OG image logic (`/images/og-image.jpg`).
- **Structured Data (JSON-LD Schema):** 
  - Developed `src/lib/schema.ts` utilities providing `LegalService`, `BreadcrumbList`, and `FAQPage` schemas.
  - Interfaced schemas into the homepage (`FAQSection.tsx`) and dynamic practice area pages ensuring excellent SERP comprehension.

## Phase 5: Forms & Contact Setup
- **Contact Page (`/contact`):** Created a dedicated contact page showcasing chamber location details, an interactive map placeholder, and contact information, encapsulated within the `<ScrollReveal />` wrapper.
- **Appointment Form:** 
  - Developed `AppointmentForm.tsx` ensuring a streamlined intake experience.
  - Implemented strong client-side validation using **React Hook Form** paired with **Zod** schema (validating name, 10-digit phone number, matter type, and description length).
- **WhatsApp Integration (`lib/whatsapp.ts`):** 
  - Instead of a traditional backend email API route, built a WhatsApp deep-link generator.
  - Upon valid form submission, the system formats the intake data (Name, Phone, Email, Matter, Description) into a clean markdown-style message and directly opens a pre-filled WhatsApp chat to the advocate's configured number, aligning with the "Direct Access" ethos.

## Phase 6: Blog System
- **Markdown Architecture:** 
  - Implemented a server-side Markdown blog system using `gray-matter` for frontmatter parsing and `react-markdown` for rendering content.
  - Added `@tailwindcss/typography` to style the rendered markdown seamlessly within the site's design system.
- **Dynamic Routes & SSG:** 
  - Created `/blog` index page to list all localized articles.
  - Developed the dynamic route `/blog/[slug]` using Static Site Generation (SSG) via `generateStaticParams`.
- **Bilingual Content Structure:** Configured `src/lib/blog.ts` to scan `content/blog/` and match files based on their localized filename extensions (e.g., `post.en.md`, `post.hi.md`), allowing independent localized SEO metadata (`title`, `excerpt`, `author`) per post.

## Phase 7: Optimization & Final Polish (Completed)
- Verified flawless responsive behavior across all sections (Home, Contact, Blog, Practice Areas) utilizing Tailwind CSS grid and flex utilities.
- Validated all Next.js 15 routing types, successfully executing a production build (`npm run build`) with zero TypeScript errors or warnings.
- Fully rewrote the `README.md` containing accurate project context, architecture details, and comprehensive local development and deployment instructions.
- The platform is now fully optimized, styled, and structurally sound for production deployment.

## SEO Phase 1: Technical & Compliance Fixes (Completed)
- **NAP single source of truth (`src/lib/business.ts`):** created with name, phone, email, hours, and both chamber locations (lat/lng/address). Refactored Footer, TopBar, WhatsAppFloat, LocationSection, HeroSection, CTABanner, FAQSection, PracticeAreas, ProcessSection, AppointmentForm, and the about/contact/practice-area pages to import from it instead of hardcoding. This also fixed wrong Kaiserbagh/Madiyaon coordinates that had drifted out of sync on the About and Contact pages, and corrected the Kaiserbagh PIN to 226001.
- **`generateLegalServiceSchema` fix (`src/lib/schema.ts`):** removed the self-serving `aggregateRating` block (4.8/13, sourced from on-site testimonials — a Google structured-data policy risk with no verifiable third-party source). Added a `location` array with both chambers as `Place` entities. Linked `founder` to the `Person` schema via a shared `@id` (`https://advocatelucknow.in/#person`). Left a `// TODO` for `sameAs` profile URLs (GBP, Justdial, Vakilno1, Lawrato, LinkedIn) once the user provides them.
- **Homepage H1 + heading hierarchy:** `HeroSection`'s `<h1>` now contains both the name and the descriptor "Criminal, Civil & Family Lawyer in Lucknow" (Hindi equivalent added) — previously the H1 was just the name. Reordered `HeroSection` before `ServiceCarousel` in `page.tsx`. Demoted `ServiceCarousel`'s slide titles from `<h2>` to styled `<p>` tags (marketing copy, not document structure) — confirmed via build output: exactly one `<h1>` on the page, containing "Lawyer in Lucknow" (EN) / "वकील" (HI).
- **Contradictions & BCI-risk copy:** unified years-of-practice to 10+ everywhere (was contradicting 15+); unified the "500+" stat to "Clients Served" (was split between "Cases Handled" and "Clients Served"); removed banned/risky terms ("Expert"/"expertise", "aggressive", comparison-with-other-lawyers phrasing) from `whyChoose`, `team`, and the civil-law carousel slide, en+hi; rewrote the criminal-law meta description to drop the "bail hearings within 24–48 hrs" outcome/timeline promise and cite the current Section 528 BNSS instead of only the superseded Section 482 CrPC. Testimonials: kept all 13 (confirmed real/verbatim by the client), but rewrote the specific clauses promising or stating a case outcome or timeline (e.g. "got bail when others said impossible", "acquittal on most counts", "48 ghante mein ho gaya", "100% trustworthy") into service-quality statements — rest of each testimonial left untouched.
- **`LegalDisclaimer` component:** new shared component (`src/components/shared/LegalDisclaimer.tsx`) rendering the localized BCI disclaimer text. Added above the footer on the homepage, all practice-area pages, about, and contact.
- **`scripts/bci-check.mjs`:** new compliance linter scanning `messages/*.json` and `content/**/*.md` for banned terms (English + Hindi), with a `bci-allow` line-level whitelist marker. Wired up as `npm run bci-check`; currently reports zero hits.
- **Sitemap automation:** new `src/lib/routes.ts` exporting `PUBLIC_PATHS` (static paths + blog slugs read from the filesystem). Rewrote `next-sitemap.config.js` to expand these paths × `[en, hi]` with correct priority and full hreflang alternates per URL — the previous hand-maintained list was missing `/blog` and the blog post entirely. Verified via build: all 18 URLs (9 paths × 2 locales) present with clean hreflang.
- **Hero carousel performance pass:** first slide keeps `priority` and adds `fetchPriority="high"`; remaining slides get explicit `loading="lazy"`. Capped `next.config.ts` `images.deviceSizes` to a max of 1920 (was defaulting up to 3840) to avoid oversized image requests on ultra-wide/4K viewports for the `sizes="100vw"` fill images.
- **Domain redirect:** `lawyerlucknow.in` → `advocatelucknow.in` 301 redirect needs to be configured at the Vercel/DNS level — this is a human task, not code (see Task 1.9 instructions given to the user).
- **Robots.txt:** confirmed still correct after build — allows `/`, references the sitemap.
- Full `npm run build` passes with zero TypeScript errors after every task in this phase.

## SEO Phase 2: Service Pages — English Money Pages (Completed)
- **Infrastructure:** `src/lib/services.ts` — a content collection for `content/services/*.md`, mirroring `blog.ts`'s `slug.locale.md` convention, with a frontmatter contract (`title`, `description`, `h1`, `updated`, `related[]`, `faqs[]`). New route `src/app/[locale]/services/[slug]/page.tsx` (SSG via `generateStaticParams`) with `generateMetadata` (canonical + hreflang + OG), `BreadcrumbList`/`FAQPage`/`Service` JSON-LD (new `generateServiceSchema` helper in `schema.ts`), markdown body via `react-markdown` + typography prose classes matching the blog's styling, a WhatsApp/call CTA sidebar, FAQ accordion, related-services links, and `LegalDisclaimer`. New `/{locale}/services` index page. Until a Hindi file exists for a slug, `/hi/services/[slug]` renders the English content with a "हिंदी संस्करण जल्द आएगा" note rather than 404ing (per user's choice) — keeps the URL live and indexable ahead of Phase 5. `routes.ts` and `next-sitemap.config.js` updated to include `/services` and derive service slugs from the filesystem.
- **12 service pages written** (all English-only for now, Phase 5 adds Hindi): `bail-lawyer-in-lucknow`, `anticipatory-bail-lawyer-lucknow`, `fir-quashing-lawyer-lucknow`, `divorce-lawyer-in-lucknow`, `mutual-consent-divorce-lucknow`, `498a-defence-lawyer-lucknow`, `cheque-bounce-lawyer-lucknow`, `property-dispute-lawyer-lucknow`, `child-custody-lawyer-lucknow`, `maintenance-lawyer-lucknow`, `domestic-violence-lawyer-lucknow`, `court-marriage-lucknow`. User confirmed the advocate takes cheque bounce and court marriage matters (both were conditional pages), confirmed NDPS/POCSO can be mentioned on the bail page, and confirmed the "fee discussed transparently before engagement" generic phrasing (no fixed fee stated anywhere). Each page: 850–960 words body (excluding FAQs), one `<h1>`, 6–8 FAQs, 2–4 contextual internal links, statutes cited "new (earlier old)" per the Master Plan §4 table only, zero fabricated case results or timelines-as-promises. Where the fact sheet didn't cover a specific statute (child custody within divorce proceedings), used the safe generic phrasing the phase file specified rather than guessing a section number.
- **`npm run bci-check` passes with zero hits** across the whole repo after this phase, including all 12 new content files (one "guaranteed"/"guarantee"/"best" hit per file was caught and rephrased during writing, each without losing legal accuracy).
- **Internal linking:** `PracticeAreas.tsx` sub-item cards that have a dedicated service page now link there directly (Bail Applications, Anticipatory Bail, FIR Defense & Quashing, Property Disputes, Divorce, Child Custody, Maintenance, Domestic Violence); cards without a page keep linking to their parent hub. Each practice-area hub page gained a "Detailed Service Pages" grid linking its children. Footer gained a new "Services" column (grid widened from 4 to 5 columns) listing 8 of the 12 service pages.
- Full `npm run build` passes with zero TypeScript errors; sitemap confirmed to include all 12 service page URLs.
- **Still open:** Hindi versions of all 12 service pages (Phase 5). `sameAs` directory URLs (Justdial, Vakilno1, Lawrato, LinkedIn) still pending from the user.

## SEO Phase 3: Location & Court Pages (Completed)
- **Infrastructure:** `src/lib/locations.ts` — a small fixed registry of exactly 4 entries (2 chambers, 2 courts; explicitly NOT a per-locality page pattern, which the phase file flags as a Google spam risk). New route `src/app/[locale]/locations/[slug]/page.tsx` (SSG via `generateStaticParams`), content sourced from a new `locations.*` namespace in `messages/en.json` (JSON fits better than markdown here since these pages are shorter/more structured than services). Each page: breadcrumb, H1, intro, body sections, embedded Google Map (`loading="lazy"`) with a "Get Directions" link, related-services grid, FAQ accordion, and `LegalDisclaimer`. `routes.ts` and `next-sitemap.config.js` updated with the 4 location slugs.
- **Schema split, verified correct:** chamber pages (`madiyaon-chamber`, `kaiserbagh-chamber`) get a new `generateChamberSchema` helper in `schema.ts` — the same `LegalService` `@id` scoped to that one chamber's `PostalAddress`/`GeoCoordinates`/`hasMap`. Court pages (`district-court-lucknow`, `high-court-lucknow-bench`) get **only** `BreadcrumbList` — confirmed via build output that court pages carry no `hasMap`/geo tying the court to the business, per the phase file's explicit instruction not to mark up a court as the advocate's location.
- **Court pages carry a visible disclaimer line** ("This is an informational page by a practising advocate. For official information, please visit the court's own website.") so they never read as an official court page.
- **High Court Lucknow Bench coordinates:** rather than hardcode lat/lng from memory (Master Plan: never guess geo facts), the map embed uses a place-name query (`"High Court of Judicature at Allahabad Lucknow Bench, Vibhuti Khand, Gomti Nagar, Lucknow"`) so Google resolves the exact pin instead of risking a wrong coordinate.
- **Task 3.4 — Areas Served:** extended `LocationSection.tsx` on the homepage with a one-paragraph "Areas We Serve in Lucknow" block listing the 12 localities from the phase file as plain text (no per-locality claims, no separate pages), plus "View chamber details" links from each chamber card to its new location page. Added the Hindi translation immediately (not deferred to Phase 5) since this section renders on the homepage in both locales right now and would otherwise throw a missing-message error on `/hi`.
- **Task 3.5 — Contact page upgrade:** added "Get Directions" and "View Chamber Details" links to both chamber cards, and a `ContactPage` JSON-LD block alongside the existing `BreadcrumbList`. Existing `AppointmentForm` left untouched.
- User answered the phase's blocking questions: one GBP only (Website field → homepage, not a chamber page — noted in `ACTION-ITEMS.md`), both chambers are "by appointment" with no fixed weekly pattern, no chamber photos yet (left `<!-- TODO photo -->` placeholders in the page), and the Madiyaon approach/route description was kept generic (no invented directions).
- Caught and fixed 3 of 4 location meta descriptions that were over the 155-char limit (159, 163, 159 chars) during the Definition-of-Done pass.
- `npm run bci-check` passes with zero hits; `npm run build` passes with zero TypeScript errors; sitemap confirmed to include all 4 location URLs × 2 locales.
- **Not committed to git this phase** — user asked to hold off on commits; all changes are in the working tree for review.
- **Still open:** chamber photos (2–3 per chamber, exterior + interior) whenever available; Hindi content for the 4 location pages (Phase 5, per the phase's own "Localize to Hindi in Phase 5" instruction — English-only for now, same pattern as service pages).

## SEO Phase 4: Legal Guides — Repurposing the Blog (Completed)
- **Renamed blog → guides throughout:** `src/lib/blog.ts` → `src/lib/guides.ts` (`BlogPost` → `Guide`, extended frontmatter contract: `title`, `description` ≤155c, `h1`, `updated`, `reviewedBy`, `topic` (`Criminal`/`Family`/`Civil`, drives the index page grouping), `faqs`, `related` — now pointing at **service slugs** to cross-link, not other guides). Route `src/app/[locale]/blog` → `src/app/[locale]/guides`; content dir `content/blog` → `content/guides`. Added a lightweight `extractHeadings()` helper that pulls H2s out of the markdown body to build the table of contents, and a `slugify` match in the guide page's `ReactMarkdown` `h2` component override so TOC anchors actually land on the right heading (no `rehype-slug` dependency added — matched the project's existing "no new deps for a solved problem" pattern from Phases 2–3).
- **Guide page template** (`src/app/[locale]/guides/[slug]/page.tsx`): breadcrumb (Home → Legal Guides → title) · H1 · "Last reviewed: {updated} · Reviewed by {reviewedBy}, Advocate, High Court Lucknow Bench" byline · sidebar table of contents built from H2s · markdown body · FAQ accordion · "Need Help With This? See Our Services" related-service cards (resolved from the guide's `related` service slugs via `getServiceBySlug`) · `<LegalDisclaimer/>`. Same `/hi` fallback pattern as services (renders English content with a "हिंदी संस्करण जल्द आएगा" banner) until Phase 5 Hindi content lands for the 7 new guides.
- **JSON-LD per guide:** `BreadcrumbList`, `FAQPage`, and a new `Article` block (`headline`, `dateModified: updated`, `author: {'@id': '.../#person'}`, `publisher: {'@id': siteUrl}`, `inLanguage`) — linked to the existing Person/LegalService `@id`s from Phase 1 rather than duplicating organization data.
- **Redirects** in `next.config.ts`: `/:locale/blog` → `/:locale/guides` (permanent), plus a specific `/:locale/blog/bail-process-in-india` → `/:locale/guides/bail-process-in-lucknow-courts` redirect *before* the generic `/:locale/blog/:slug` → `/:locale/guides/:slug` passthrough, since the one existing post's slug changed as part of its Task 4.2 G2 upgrade. Verified both with `curl -I` against the dev server: 308 permanent redirects landing on the correct new URLs.
- **Nav:** `nav.blog` message key was already "Legal Guides" / "कानूनी गाइड" from an earlier pass, but neither Navbar nor Footer actually rendered a link to it — added `<Link href="/guides">{t('blog')}</Link>` to both (desktop nav, mobile nav, and Footer's Quick Links column).
- **Guides index page** (`src/app/[locale]/guides/page.tsx`): intro paragraph + card grid grouped under "Criminal Law" / "Family Law" / "Civil Law" headings (order fixed via a `TOPIC_ORDER` array; a topic section only renders if it has guides). No dates on cards, per the phase file's explicit "not a blog" framing. Same English-fallback listing pattern as the services index (`/hi/guides` lists the EN set so it isn't empty pre-Phase-5).
- **Sitemap/routes:** `src/lib/routes.ts` and `next-sitemap.config.js` both updated in lockstep (`/blog` → `/guides`, slugs now read from `content/guides/`) — confirmed via build output: all 8 guide slugs × 2 locales present in `public/sitemap.xml`, at priority 0.75.
- **8 guides written** (English; Hindi is Phase 5, except the migrated G2 which got both locales since it already existed in Hindi and leaving stale pre-BNSS content live wasn't acceptable):
  - `how-to-file-fir-in-lucknow` (G1) — Section 173 BNSS FIR procedure, Zero FIR, e-FIR via UPCOP, refusal escalation ladder (SP under S.173(4) BNSS → Magistrate under S.175(3) BNSS)
  - `bail-process-in-lucknow-courts` (G2) — migrated from the old `bail-process-in-india` post (slug changed, redirect added); rewrote the entire pre-BNSS content (was still citing S.438/439/437 CrPC only) into the current BNSS framework with a worked default-bail example; **only guide with a Hindi version**, also rewritten from its stale CrPC-only Hindi original
  - `divorce-procedure-lucknow-family-court` (G3) — contested vs mutual consent comparison table, stage-by-stage flow, mediation, interim relief, documents checklist
  - `rights-on-arrest-police-questioning-up` (G4) — Article 22, S.180 BNSS statement rights, S.35 BNSS pre-arrest notice regime, women's arrest safeguards, first-24-hours family checklist, 112/1090/181 helplines
  - `maintenance-laws-india-wife-children-parents` (G5) — S.144 BNSS vs HMA 24/25 vs DV Act S.20 overlapping-forum comparison table, *Rajnesh v. Neha* income-affidavit regime, enforcement
  - `property-partition-guide-up` (G6) — coparcenary vs self-acquired, daughters' equal coparcenary rights post-2005 amendment stated as settled law, partition suit's two-decree stages, khatauni/mutation revenue-record realities in UP
  - `cheque-bounce-case-guide` (G7) — built since P7 (`cheque-bounce-lawyer-lucknow`) exists; full S.138 NI Act timeline (30-day notice / 15-day payment / 1-month complaint), demand-notice defects, S.143A interim compensation
  - `ipc-to-bns-what-changed` (G8) — the internal-link-magnet reference page; renumbering table (FIR, arrest notice, anticipatory/regular/default bail, S.482→528 quashing, S.125→144 maintenance, 498A→85-86 BNS) that every other criminal guide/service page's "new (earlier old)" citations trace back to; states "old cases continue under old codes" clearly
  - All facts (statute numbers, procedural steps, safeguards) were sourced from the already-vetted Phase 2 service-page content rather than re-derived from memory, to avoid drift between what a guide says and what its linked service page says.
- **`npm run bci-check` passes with zero hits** — caught and fixed 5 hits during writing/review (English "guarantee" ×1 and "best" ×2, Hindi "गारंटी" ×2), each rephrased without losing meaning.
- **Bug caught in Definition-of-Done pass:** G8's frontmatter had a YAML parse error (unescaped nested double-quotes around "renumber" inside a double-quoted FAQ answer) that 500'd the guides index page at build/runtime since `getAllGuides` eagerly parses every file; fixed by removing the inner quotes. Also caught a wrong translation key (`common('bookNow')`, which doesn't exist) in the guide template's sidebar CTA — corrected to `common('bookConsultation')`. Both were only caught by actually hitting the pages with curl against a running dev server, not by `npm run build` alone (SSG only renders pages reachable via `generateStaticParams`, and the runtime error path differs slightly from build-time).
- Full `npm run build` passes with zero TypeScript errors; `npm run bci-check` passes with zero hits; all `/blog` → `/guides` redirects verified with `curl -I` (308 permanent, correct destination including the slug-change case).
- **Not committed to git this phase** — following the same working-tree-for-review pattern as Phase 3, pending user sign-off.
- **Still open:** Hindi content for 7 of the 8 guides (Phase 5); Google Search Console indexing requests for the 8 new guide URLs once deployed (added to `ACTION-ITEMS.md`).

## SEO Phase 5: Hindi SEO — Native Search-Language Content (Completed)
- **Translation philosophy applied, not literal translation:** every Hindi page was rewritten section-by-section using the same facts, statute numbers, and internal links as its English counterpart, but re-asked and re-phrased the way Hindi/Hinglish speakers actually search — common English legal words kept transliterated in Devanagari (जमानत, एफआईआर/FIR, कस्टडी, वकील), statute numbers in Latin digits, act abbreviations (BNSS, CrPC, IPC, BNS, NI Act) left in Latin script, "धारा" for "Section." FAQs were re-asked as natural Hindi search queries ("बेल कितने दिन में मिलती है", "जमानत कितने दिन में मिलती है") rather than translated English questions.
- **12 Hindi service pages written** (`content/services/*.hi.md`): `bail-lawyer-in-lucknow`, `anticipatory-bail-lawyer-lucknow`, `fir-quashing-lawyer-lucknow`, `divorce-lawyer-in-lucknow`, `mutual-consent-divorce-lucknow`, `498a-defence-lawyer-lucknow`, `cheque-bounce-lawyer-lucknow`, `property-dispute-lawyer-lucknow`, `child-custody-lawyer-lucknow`, `maintenance-lawyer-lucknow`, `domestic-violence-lawyer-lucknow`, `court-marriage-lucknow` — closing out the `/hi/services/*` fallback-banner behavior from Phase 2 for every slug.
- **7 Hindi guides written** (`content/guides/*.hi.md`): `how-to-file-fir-in-lucknow`, `divorce-procedure-lucknow-family-court`, `rights-on-arrest-police-questioning-up`, `maintenance-laws-india-wife-children-parents`, `property-partition-guide-up`, `cheque-bounce-case-guide`, `ipc-to-bns-what-changed` — the 8th guide (`bail-process-in-lucknow-courts`) already had Hindi from its Phase 4 migration.
- **Task 5.4 — Hindi `locations` namespace added to `messages/hi.json`:** this didn't exist at all before this phase — `/hi/locations/*` would have thrown `MISSING_MESSAGE` errors at runtime the first time anyone visited one (the route reads `locations.${slug}` and `locations.labels` directly via `getTranslations`). Added the full `labels` block plus all 4 location entries (`madiyaon-chamber`, `kaiserbagh-chamber`, `district-court-lucknow`, `high-court-lucknow-bench`) mirroring the English structure exactly (metaTitle, metaDescription, h1, intro, sections, areasServed, faqs).
- **Task 5.1 — homepage Hindi audit, not a blind rewrite:** read every homepage section (`hero`, `practiceAreas`, `faq`, `team`, `about`, `contact`, `carousel`, `trustStrip`, `ctaBanner`, `process`, `reviews`, `areasServed`, `disclaimer`) against the glossary. Most sections were already natural, correct search-Hindi and needed no changes. Found and fixed real drift instead: several homepage practice-area cards and FAQ answers, in **both English and Hindi**, were still citing only the pre-BNSS section numbers (`Section 438 CrPC`, `Section 482 CrPC`, `Section 125 CrPC`, `Section 156(3) CrPC`, `Section 161 CrPC`, `Section 41 CrPC`, `Section 320 CrPC`) instead of the site's established "new BNSS (earlier old CrPC)" citation pattern already used on every Phase 2–4 page. Confirmed with the user this was worth fixing in both locales now rather than deferring, since it's a factual/compliance issue, not just a phrasing one — updated ~20 individual citations across `messages/en.json` and `messages/hi.json` (homepage cards, homepage FAQ, and the `areas`/`areaFaq` practice-area-detail content that had the same drift, confirmed in scope by the user before touching it).
- **Title-length pass (Task 5.5):** checked every Hindi frontmatter `title` using actual Unicode character count (not byte length, which shell `${#var}` reports incorrectly for Devanagari) — found one guide title at 61 characters and trimmed it to fit the ≤60c target; all descriptions confirmed ≤155 characters.
- **Verification:** started the dev server and hit every new `/hi/services/*`, `/hi/guides/*`, and `/hi/locations/*` URL plus all 4 `/hi/practice-areas/*` pages directly with `curl` (not just `npm run build`, which would not have caught a `MISSING_MESSAGE` runtime error on the locations route) — all returned 200 with zero errors or warnings in the server log. Confirmed hreflang pairs resolve both directions (`en-IN`/`hi-IN`/`x-default`) with matching absolute URLs on a sample service page. Confirmed sitemap contains the correct counts: 13 `hi/services` URLs (12 pages + index), 9 `hi/guides` URLs (8 pages + index), 4 `hi/locations` URLs.
- **`npm run bci-check` passes with zero hits** across all new Hindi content; `npm run build` passes with zero TypeScript errors.
- **Not committed to git this phase** — following the same working-tree-for-review pattern as Phases 3–4, pending user sign-off.
- **Still open:** user (native speaker) has not yet spot-reviewed any pages, per the phase's own exit criterion — flagged as a manual action item rather than skipped, since Claude cannot substitute for a native-speaker language review. No `⚠ OPEN QUESTION (language)` items were raised during writing — the glossary terms and existing site conventions (from Phases 1–4's Hindi copy) were specific enough to avoid guessing on phrasing.
